---
description: Execute a written implementation plan with review checkpoints, following each task step by step.
agent: build
---

Load the `executing-plans` skill and follow its complete workflow to execute the plan: $ARGUMENTS

Load and critically review the plan, raise any concerns before starting, create a todo list from all tasks, execute each task following its bite-sized steps exactly, run verifications, then finish the development branch.