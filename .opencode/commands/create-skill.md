---
description: Scaffold a new agent skill following repository conventions.
agent: build
---

Load the `create-skill` skill and follow its workflow to scaffold the new skill for: $ARGUMENTS

Follow the naming rules and anatomy defined in the skill, generate the canonical `.agents/skills/<name>/SKILL.md`, the `.opencode/skills/<name>/SKILL.md` thin wrapper, and the optional `.opencode/commands/<name>.md`, then run the checklist before finishing.
