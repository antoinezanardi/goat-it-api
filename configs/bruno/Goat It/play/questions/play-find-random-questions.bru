meta {
  name: Get Random Questions
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/questions/search/random
  body: json
  auth: inherit
}

body:json {
  {
    "limit": 20
  }
}

settings {
  encodeUrl: true
  timeout: inherit
  followRedirects: true
  maxRedirects: 5
}
