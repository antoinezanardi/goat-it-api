meta {
  name: Remove Theme From Question
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/admin/questions/:id/themes/:themeId
  body: json
  auth: inherit
}

params:path {
  themeId: {{questionThemeId}}
  id: {{questionId}}
}

body:json {
  {}
}

settings {
  encodeUrl: true
  timeout: inherit
  followRedirects: true
  maxRedirects: 5
}
