meta {
  name: Get Questions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/questions
  body: none
  auth: inherit
}

params:query {
  ~sort-by: createdAt
  ~sort-order: desc
}

settings {
  encodeUrl: true
  timeout: inherit
  followRedirects: true
  maxRedirects: 5
}
