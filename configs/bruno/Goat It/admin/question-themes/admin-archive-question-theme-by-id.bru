meta {
  name: Archive Question Theme By Id
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/admin/question-themes/:id/archive
  body: none
  auth: inherit
}

params:path {
  id: {{questionThemeId}}
}

settings {
  encodeUrl: true
  timeout: inherit
  followRedirects: true
  maxRedirects: 5
}
