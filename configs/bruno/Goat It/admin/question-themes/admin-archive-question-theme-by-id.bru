meta {
  name: Archive Question Theme By Id
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/admin/question-themes/:id/archive
  body: json
  auth: inherit
}

params:path {
  id: {{questionThemeId}}
}

body:json {
  {}
}

settings {
  encodeUrl: true
  timeout: inherit
  followRedirects: true
  maxRedirects: 5
}
